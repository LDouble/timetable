# 项目概述
- Flutter / Dart 3.9 包项目，用于复刻开源项目 ColorTimetable 的课程表组件（参考源码保存在 `ColorTimetable-main/` 目录）
- 目前 `lib/timetable.dart` 仍是模板代码，需要在其中实现组件；`test/` 目录包含默认生成的单元测试示例
- 依赖精简，仅引用 Flutter SDK，使用 `flutter_lints` 进行静态检查
- README 仍是模板，需要在后续迭代中补充用法、截图等信息