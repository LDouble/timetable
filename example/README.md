# Timetable Example

此示例演示如何在 Flutter 应用中集成 `ColorTimetable` 组件。

## 运行步骤

```bash
cd example
flutter pub get
flutter run
```

## 功能

- 复刻 ColorTimetable 的课程表 UI
- 浅色/深色主题切换
- 点击课程显示 SnackBar，同步触发内置详情面板
```}